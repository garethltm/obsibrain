Activations at hidden layers can be viewed as features extracted as functions of inputs
- features in the hidden layers are different from the input features (attributes)

Every hidden layer represents